<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.identitymanager">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IdentityManager">


        <receiver
            android:name=".receivers.NotificationReceiver"
            android:enabled="true"
            android:exported="false"></receiver>

        <service
            android:name=".services.NotificationService"
            android:enabled="true"
            android:exported="true"></service>

        <activity
            android:name=".fragments.AccountListFragment"
            android:exported="false" />
        <activity
            android:name=".activities.InstagramToChangePasswordsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.FacebookToChangePasswordsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.YoutubeToChangePasswordsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.InstagramWeakPasswordsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.FacebookWeakPasswordsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.YoutubeWeakPasswordsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.MainActivity" />
    </application>

</manifest>